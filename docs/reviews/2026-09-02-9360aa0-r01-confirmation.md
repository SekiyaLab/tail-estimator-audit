# Confirmation — Tail Estimator Audit R01

- **Reviewer / model:** Codex (GPT-5), fresh confirmation reviewer.
- **Reviewed correction:** `9360aa00503411544bbabdce7c03ae44a0086301`.
- **Prior review:** `docs/reviews/2026-09-02-75714e8-forensic-review.md`
  (`TAIL-R01-001`).
- **Scope:** confirm only truthful denominator/failure terminology and that no
  substantive evidence changed.

## Evidence checked

- `git diff 2bd069c..9360aa0` changes only `README.md` and
  `docs/research_contract.md`.
- The correction now distinguishes literal GPD optimiser failures from a
  successful fit with `xi_hat <= 0`, which the study defines as a
  heavy-tail-recovery failure.
- The revised README and contract state that failure rate is calculated over
  all replications while bias, RMSE, quantile-error, and coverage are
  conditional on finite fits and expose `n_used`/`n_dropped`.
- The previously inspected Student-t / tail-index 6 / `n=500` cell remains
  consistent with that language: 400 attempted replications, 201 finite
  scoring fits, 199 dropped fits.
- No DGP, estimator, metric, seed, raw result, test, figure, or dependency
  file changed.

## Verdict

**ACCEPT.** `TAIL-R01-001` is resolved. The correction is minimal and makes
the operational failure rate and conditional estimator-quality metrics
scientifically honest without changing the study's evidence.

## Next boundary

The local Track B history through this confirmation is accepted as
public-ready pending a separate explicit publication decision. This confirms
neither a remote push nor a personal-site change.
